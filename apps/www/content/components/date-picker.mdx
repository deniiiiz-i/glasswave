---
title: Date Picker
description: Popover trigger + single-date Calendar.
---

import { DatePickerPreview } from "@/components/previews/date-picker-preview";

<DatePickerPreview />

## Installation

<Installation name="date-picker" />

## Usage

```tsx
import { DatePicker } from "glasswave";

<DatePicker
  date={date}
  onDateChange={setDate}
  placeholder="Pick a date"
  formatPattern="PPP"
/>
```

## API Reference

### DatePicker

| Prop            | Type                                | Default         |
| --------------- | ----------------------------------- | --------------- |
| `date`          | `Date`                              | —               |
| `onDateChange`  | `(date: Date \| undefined) => void` | —               |
| `placeholder`   | `string`                            | `"Pick a date"` |
| `align`         | `"start" \| "center" \| "end"`      | —               |
| `formatPattern` | `string`                            | —               |
| `disabled`      | `boolean`                           | `false`         |
